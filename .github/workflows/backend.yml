name: Build & Push Backend with Docker Compose

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: us-east-1
      ECR_PUBLIC_URI: public.ecr.aws/e0b3f6o5/db-publice-backend
      IMAGE_TAG: ${{ github.sha }}

    steps:
      # 1. Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Configure AWS credentials
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # 3. Show Docker version
      - name: Show Docker version
        run: docker --version

      # 4. Build containers with Docker Compose (no cache)
      - name: Build containers
        run: |
          docker compose build --no-cache

      # 5. Tag backend image
      - name: Tag backend image
        run: |
          # Ensure your docker-compose.yml specifies image name: react_backend_api for backend
          docker tag react_backend_api:latest $ECR_PUBLIC_URI:$IMAGE_TAG
          docker tag react_backend_api:latest $ECR_PUBLIC_URI:latest

      # 6. Login to ECR Public
      - name: Login to Amazon ECR Public
        run: |
          aws ecr-public get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin public.ecr.aws

      # 7. Push backend image to ECR Public
      - name: Push backend image
        run: |
          docker push $ECR_PUBLIC_URI:$IMAGE_TAG
          docker push $ECR_PUBLIC_URI:latest
